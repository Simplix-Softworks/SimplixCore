package dev.simplix.core.common.permission;

public enum PermissionType {
  //Access a command, etc
  COMMAND,
  //Take actions with a menu
  MENU,
  //Bypass permissions in chat, etc
  BY_PASS,
  OTHER
}
